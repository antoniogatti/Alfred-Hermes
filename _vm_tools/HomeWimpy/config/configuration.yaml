# Home Assistant - HomeWimpy
# Generated by Alfred

homeassistant:
  name: HomeWimpy
  latitude: 40.6417     # update with your actual coordinates
  longitude: 17.9175    # Brindisi area as placeholder
  elevation: 15
  unit_system: metric
  currency: EUR
  country: IT
  language: it
  time_zone: Europe/Rome

# Enable frontend
frontend:

# Enable HTTP
http:
  server_host: "0.0.0.0"
  server_port: 8123

# Enable config validation
config:

# Enable the recorder (history / logbook)
recorder:

# Enable history
history:

# Enable logbook
logbook:

# Enable energy panel
energy:

# Enable map
map:

# Enable Sun integration
sun:

# Enable system health
system_health:

# Enable person tracking
person:

# Enable zone support
zone:

# Enable scripts and automations
automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
